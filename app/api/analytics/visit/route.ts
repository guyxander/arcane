import { NextResponse } from "next/server";
import { createAdminClient } from "@/lib/supabase/admin";
export async function POST(request:Request){try{const body=await request.json();const ua=request.headers.get("user-agent")||"";const browser=/Edg\//.test(ua)?"Edge":/Chrome\//.test(ua)?"Chrome":/Firefox\//.test(ua)?"Firefox":/Safari\//.test(ua)?"Safari":"Other";const os=/Android/.test(ua)?"Android":/iPhone|iPad/.test(ua)?"iOS":/Windows/.test(ua)?"Windows":/Mac OS/.test(ua)?"macOS":/Linux/.test(ua)?"Linux":"Other";const db=createAdminClient();if(db)await db.from("visitor_events").insert({path:String(body.path||"/").slice(0,250),device_type:String(body.device||"unknown"),browser,os,referrer_domain:request.headers.get("referer")?new URL(request.headers.get("referer")!).hostname:null});return new NextResponse(null,{status:204});}catch{return new NextResponse(null,{status:204});}}
